name: field_visit_log
description: A Flutter Field Visit Log application.
publish_to: 'none'

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^9.1.0
  sqflite: ^2.4.2+1
  path: ^1.9.0
  connectivity_plus: ^6.1.3
  equatable: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true