import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand color
  static const Color primary = Color(0xFFE95A0C);
  static const Color onPrimary = Color(0xFFFFFFFF);

  // Backgrounds
  static const Color background = Color(0xFFF7F4EC);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color foreground = Color(0xFF171512);
  static const Color muted = Color(0xFF746F67);

  // Borders
  static const Color border = Color(0xFFD8CCB8);
  static const Color line = Color(0xFFD8CCB8);

  // Interactive
  static const Color hover = Color(0xFFF2EDE3);

  // Semantic colors
  static const Color success = Color(0xFF2E9B5B);
  static const Color warning = Color(0xFFE5A11A);
  static const Color error = Color(0xFFE24A4A);
  static const Color info = Color(0xFFE95A0C);

  // Shimmer
  static const Color shimmerBase = Color(0xFFE5DED1);
  static const Color shimmerHighlight = Color(0xFFF7F4EC);
}
